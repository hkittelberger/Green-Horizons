using UnityEngine;
using Unity.Netcode;

public class GameVisualManager : MonoBehaviour
{
    // [SerializeField] private Transform cityPrefab;
    // [SerializeField] private Transform settlementPrefab;
    // [SerializeField] private Transform solarPanelPrefab;
    // [SerializeField] private Transform powerPlantPrefab;
    // [SerializeField] private Transform farmPrefab;
    // [SerializeField] private Transform hurricaneShieldPrefab;

    // private void Start()
    // {
    //     GameManager.Instance.OnClickedOnBuild += GameManager_OnClickedOnBuild;
    // }

    // private void GameManager_OnClickedOnBuild(object sender, GameManager.OnClickedOnBuildEventArgs e)
    // {
    //     Transform spawnedBuilding = Instantiate(cityPrefab, e.Tile.transform.position, Quaternion.identity);
    //     spawnedBuilding.GetComponent<NetworkObject>().Spawn(true);
    // }
}

